/**
 * Saving role for training inst if save button pressed
 * and changing colour of new saved role card to green
 */


$(".saveRoleButton").on('click', event => {
    var id = $(this).val();
    alert(id);
    // <%=role._id.toString()%>
})

